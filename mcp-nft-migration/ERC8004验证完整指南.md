# ERC-8004 验证完整指南

## 概述

ERC-8004 是一个 **AI Agent 身份和信任验证标准**，用于在区块链上记录和验证 AI Agent 执行的任务。在 NFT IPFS to Filecoin 迁移项目中，我们使用 ERC-8004 来：

1. **注册 AI Agent** - 在 Sepolia 测试网上注册迁移 Agent
2. **创建验证请求** - 记录迁移任务（NFT Token 范围、IPFS CID 等）
3. **提交验证结果** - 上传迁移证明（Filecoin PieceCID、成功率等）
4. **查询验证状态** - 验证迁移是否被信任

## 🎯 当前 MCP 工具状态

### ⚠️ 注意：工具需要修复

当前 MCP 中的 ERC-8004 相关工具引用了不存在的 `phases/` 目录，**需要修复**后才能使用。

**现有工具**：
1. ❌ `erc8004_validate` - 需要修复（引用 `Phase5_ERC8004Validation`）
2. ❌ `update_contract_uri` - 需要修复（引用 `Phase4_UpdateContract`）

**实际可用的实现**：
- ✅ `mvp-demo/erc8004-client.js` - 完整的 ERC-8004 客户端

## 📋 ERC-8004 验证工作流程

### 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Agent 注册 (Agent Identity Contract - Sepolia)          │
│    - 注册 AI Agent 身份                                     │
│    - 获得 Agent ID (ERC-721 Token)                         │
│    - 元数据：名称、描述、能力等                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. NFT 扫描和上传 (Ethereum Mainnet + Filecoin)            │
│    - 扫描 Azuki NFT (Token #0-#4)                         │
│    - 提取 IPFS CID                                         │
│    - 上传元数据到 Filecoin                                  │
│    - 获得 Filecoin PieceCID                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 创建验证请求 (Agent Validation Contract - Sepolia)      │
│    - Agent ID: 注册的 AI Agent                             │
│    - Task URI: 迁移任务描述 (NFT 合约、Token 范围)          │
│    - Validator: 验证者地址                                  │
│    - 返回: Request Hash                                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 提交验证结果 (Agent Validation Contract - Sepolia)      │
│    - Request Hash: 验证请求的哈希                           │
│    - Is Valid: 迁移是否成功 (true/false)                   │
│    - Proof URI: 迁移证明 (IPFS CID of proof metadata)     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 查询验证状态                                             │
│    - 查询验证请求详情                                       │
│    - 确认迁移已验证                                         │
│    - 获取证明 URI                                           │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 实际操作指南

### 方法 1: 使用 MCP 工具（修复后）

#### 步骤 1: 注册 Agent

**注意**: 当前 MCP 没有 `register_agent` 工具，需要手动在 mvp-demo 中注册。

```bash
cd /var/tmp/vibe-kanban/worktrees/0d79-aiagent/mvp-demo
node register-agent.js
```

#### 步骤 2: 扫描 NFT

```
请使用 nft_scan 工具扫描 Azuki NFT
合约地址: 0xED5AF388653567Af2F388E6224dC7C4b3241C544
```

#### 步骤 3: 上传到 Filecoin

```
请使用 upload_to_filecoin 工具上传 Token #0 的元数据
Token ID: 0
Metadata: { ... }
Contract: 0xED5AF388653567Af2F388E6224dC7C4b3241C544
```

获得 PieceCID: `bafkz...`

#### 步骤 4: 创建验证请求

**注意**: 需要先修复 `erc8004_validate` 工具。

```
请创建 ERC-8004 验证请求
Agent ID: 1
Task URI: ipfs://QmTaskMetadata...
Validator: 0x...
```

#### 步骤 5: 提交验证

```
请提交 ERC-8004 验证结果
Request Hash: 0x...
Is Valid: true
Proof URI: ipfs://QmProofMetadata...
```

#### 步骤 6: 查询验证状态

```
请查询验证请求状态
Request Hash: 0x...
```

### 方法 2: 直接使用 erc8004-client.js（推荐）

由于 MCP 工具需要修复，当前推荐直接使用 `erc8004-client.js`：

#### 创建验证脚本

```javascript
// verify-migration.js
import 'dotenv/config';
import { ethers } from 'ethers';
import { ERC8004Client } from './erc8004-client.js';

async function main() {
  // 1. 连接 Sepolia 测试网
  const provider = new ethers.JsonRpcProvider(
    process.env.VALIDATION_NETWORK_RPC_URL
  );
  const signer = new ethers.Wallet(process.env.VALIDATOR_PRIVATE_KEY, provider);

  // 2. 初始化 ERC-8004 客户端
  const client = new ERC8004Client(
    provider,
    signer,
    process.env.AGENT_IDENTITY_ADDRESS,
    process.env.AGENT_VALIDATION_ADDRESS
  );

  // 3. 注册 Agent（如果还没注册）
  const agentMetadata = client.generateAgentMetadata(
    'NFT Migration Agent',
    'AI Agent for migrating NFT metadata from IPFS to Filecoin',
    ['https://api.example.com/migrate'],
    ['nft-scan', 'filecoin-upload', 'metadata-migration']
  );

  // 上传元数据到 IPFS
  const metadataURI = 'ipfs://QmAgentMetadata...';

  const registration = await client.registerAgent(metadataURI);
  console.log('Agent ID:', registration.agentId);

  // 4. 创建任务元数据
  const taskMetadata = client.generateTaskMetadata(
    'Migrate Azuki NFT #0-#4 from IPFS to Filecoin',
    '0xED5AF388653567Af2F388E6224dC7C4b3241C544',
    { start: 0, end: 4 },
    ['QmZcH4YvBVVRJtdn4RdbaqgspFU8gH6P9vomDpBVpAL3u4']
  );

  // 上传任务元数据到 IPFS
  const taskURI = 'ipfs://QmTaskMetadata...';

  // 5. 创建验证请求
  const request = await client.createValidationRequest(
    registration.agentId,
    taskURI,
    signer.address // 自己作为验证者
  );
  console.log('Request Hash:', request.requestHash);

  // 6. 执行迁移任务（使用 MCP 工具）
  const migrationResults = [
    {
      ipfsCid: 'QmZcH4YvBVVRJtdn4RdbaqgspFU8gH6P9vomDpBVpAL3u4/0',
      filecoinPieceCid: 'bafkzcibeucad6ecm43isurbsfynxklsgkoq6bsphu2lk5ntez4vtlqk3hhw6zcccbu',
      filecoinCarCid: 'baga...',
      success: true
    },
    // ... Token #1-#4
  ];

  // 7. 生成证明元数据
  const proofMetadata = client.generateProofMetadata(taskURI, migrationResults);

  // 上传证明到 IPFS
  const proofURI = 'ipfs://QmProofMetadata...';

  // 8. 提交验证
  const validation = await client.submitValidation(
    request.requestHash,
    true, // 迁移成功
    proofURI
  );
  console.log('Validation submitted:', validation.txHash);

  // 9. 查询验证状态
  const status = await client.getValidationRequest(request.requestHash);
  console.log('Validation Status:', status.status);
  console.log('Is Valid:', status.isValid);
  console.log('Proof URI:', status.proofURI);
}

main().catch(console.error);
```

## 📊 元数据结构

### Agent 元数据

```json
{
  "name": "NFT Migration Agent",
  "description": "AI Agent for migrating NFT metadata from IPFS to Filecoin",
  "image": "",
  "endpoints": [
    "https://api.example.com/migrate"
  ],
  "capabilities": [
    "nft-scan",
    "filecoin-upload",
    "metadata-migration"
  ],
  "supportedTrust": ["validation"],
  "version": "1.0.0",
  "timestamp": "2025-10-20T01:30:00.000Z"
}
```

### Task 元数据

```json
{
  "task": "NFT IPFS to Filecoin Migration",
  "description": "Migrate Azuki NFT #0-#4 from IPFS to Filecoin",
  "nftContract": "0xED5AF388653567Af2F388E6224dC7C4b3241C544",
  "tokenRange": {
    "start": 0,
    "end": 4
  },
  "ipfsCids": [
    "QmZcH4YvBVVRJtdn4RdbaqgspFU8gH6P9vomDpBVpAL3u4",
    "QmYDvPAXtiJg7s8JdRBSLWdgSphQdac8j1YuQNNxcGE1hg"
  ],
  "timestamp": "2025-10-20T01:30:00.000Z"
}
```

### Proof 元数据

```json
{
  "taskURI": "ipfs://QmTaskMetadata...",
  "proof": {
    "type": "FilecoinMigration",
    "migrationResults": [
      {
        "ipfsCid": "QmZcH4YvBVVRJtdn4RdbaqgspFU8gH6P9vomDpBVpAL3u4/0",
        "filecoinPieceCid": "bafkzcibeucad6ecm43isurbsfynxklsgkoq6bsphu2lk5ntez4vtlqk3hhw6zcccbu",
        "filecoinCarCid": "baga...",
        "success": true,
        "error": null
      }
    ],
    "summary": {
      "total": 5,
      "successful": 5,
      "failed": 0,
      "successRate": 100
    }
  },
  "verificationMethod": "On-chain storage proof via Synapse SDK",
  "timestamp": "2025-10-20T01:30:00.000Z"
}
```

## 🔍 验证查询

### 查询 Agent 信息

```javascript
const agent = await client.getAgent(agentId);
console.log('Agent Owner:', agent.owner);
console.log('Metadata URI:', agent.metadataURI);
console.log('Is Active:', agent.isActive);
```

### 查询验证请求

```javascript
const request = await client.getValidationRequest(requestHash);
console.log('Status:', request.status); // Pending, Completed, Expired
console.log('Is Valid:', request.isValid);
console.log('Proof URI:', request.proofURI);
console.log('Requester:', request.requester);
console.log('Validator:', request.validator);
```

## 🌐 已部署的合约

### Sepolia 测试网

| 合约 | 地址 | 用途 |
|-----|------|------|
| **Agent Identity** | `0x7177a6867296406881E20d6647232314736Dd09A` | Agent 注册和身份管理 |
| **Agent Reputation** | `0xB5048e3ef1DA4E04deB6f7d0423D06F63869e322` | 信誉系统（可选） |
| **Agent Validation** | `0x662b40A526cb4017d947e71eAF6753BF3eeE66d8` | 验证请求和提交 |

### 主要功能

**Agent Identity 合约**：
- `register(metadataURI)` - 注册 Agent，返回 Agent ID
- `ownerOf(agentId)` - 查询 Agent 所有者
- `tokenURI(agentId)` - 查询 Agent 元数据 URI
- `registrationFee()` - 查询注册费用（当前为 0）

**Agent Validation 合约**：
- `requestValidation(agentId, workURI, validator)` - 创建验证请求
- `submitValidation(requestHash, isValid, proofURI)` - 提交验证结果
- `getValidationRequest(requestHash)` - 查询验证请求详情
- `getValidationStatus(requestHash)` - 查询验证状态

## 🛠️ MCP 工具修复建议

### 需要添加的工具

1. **register_agent** - 注册 AI Agent
2. **create_validation_request** - 创建验证请求
3. **submit_validation** - 提交验证结果
4. **get_validation_status** - 查询验证状态
5. **get_agent_info** - 查询 Agent 信息

### 修复 erc8004_validate

当前代码引用不存在的 `Phase5_ERC8004Validation`，需要改为使用 `ERC8004Client`：

```typescript
// 修复前
import { Phase5_ERC8004Validation } from './phases/Phase5_ERC8004Validation.js';

// 修复后
import { ERC8004Client } from './erc8004-client.js';
import { ethers } from 'ethers';

const provider = new ethers.JsonRpcProvider(process.env.VALIDATION_NETWORK_RPC_URL);
const signer = new ethers.Wallet(process.env.VALIDATOR_PRIVATE_KEY, provider);

const client = new ERC8004Client(
  provider,
  signer,
  process.env.AGENT_IDENTITY_ADDRESS,
  process.env.AGENT_VALIDATION_ADDRESS
);

const request = await client.getValidationRequest(requestHash);
```

## 📝 完整示例

### 端到端验证流程

```bash
# 1. 扫描 NFT
node scan-azuki.js

# 2. 上传到 Filecoin
# （使用 MCP upload_to_filecoin 工具）

# 3. 注册 Agent 和创建验证
node verify-migration.js
```

输出示例：

```
📝 Registering Agent with ERC-8004...
============================================================
   Metadata URI: ipfs://QmAgentMetadata...
   Registration Fee: 0.0 ETH
   Current Balance: 1.5 ETH

   Sending registration transaction...
   Transaction hash: 0x123...
   Waiting for confirmation...

✅ Agent Registered Successfully!
   Agent ID (Token ID): 1
   Transaction: 0x123...
   Block: 5678901

📋 Creating Validation Request...
============================================================
   Agent ID: 1
   Task URI: ipfs://QmTaskMetadata...
   Validator: 0xB34d4c8E3AcCB5FA62455228281649Be525D4e59

   Sending transaction...
   Transaction hash: 0x456...
   Waiting for confirmation...

✅ Validation Request Created!
   Request Hash: 0x789abc...
   Transaction: 0x456...
   Block: 5678902

📤 Submitting Validation...
============================================================
   Request Hash: 0x789abc...
   Is Valid: true
   Proof URI: ipfs://QmProofMetadata...

   Sending transaction...
   Transaction hash: 0xdef...
   Waiting for confirmation...

✅ Validation Submitted!
   Transaction: 0xdef...
   Block: 5678903

🔍 Querying Validation Request 0x789abc...

📋 Validation Request:
   Request Hash: 0x789abc...
   Agent ID: 1
   Requester: 0xB34d4c8E3AcCB5FA62455228281649Be525D4e59
   Validator: 0xB34d4c8E3AcCB5FA62455228281649Be525D4e59
   Work URI: ipfs://QmTaskMetadata...
   Proof URI: ipfs://QmProofMetadata...
   Status: Completed
   Is Valid: true
   Requested: 2025-10-20T01:30:00.000Z
```

## 🎯 验证成功的标准

一个成功的 ERC-8004 验证应该满足：

1. ✅ **Agent 已注册** - Agent ID 存在于 Identity 合约
2. ✅ **请求已创建** - Request Hash 存在于 Validation 合约
3. ✅ **验证已提交** - Status 为 "Completed"
4. ✅ **验证通过** - isValid 为 true
5. ✅ **证明可访问** - Proof URI 指向有效的 IPFS 元数据
6. ✅ **链上可查** - 所有交易可在 Sepolia Etherscan 查询

## 🔗 相关资源

- **ERC-8004 规范**: https://eips.ethereum.org/EIPS/eip-8004
- **合约源码**: `mvp-demo/erc8004-client.js`
- **Sepolia Etherscan**: https://sepolia.etherscan.io/
- **MCP 工具**: `src/tools/validation.ts`（需要修复）

## 总结

ERC-8004 为 AI Agent 提供了**链上信任验证机制**。在 NFT 迁移项目中，它记录了：
- AI Agent 的身份和能力
- 迁移任务的详细信息
- 迁移结果的证明
- 验证者对迁移的确认

这使得 NFT 迁移过程**可验证、可追溯、可信任**，为未来的 AI Agent 协作提供了基础设施。
