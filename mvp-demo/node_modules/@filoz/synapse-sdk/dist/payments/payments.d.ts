import { ethers } from 'ethers';
import type { TokenAmount, TokenIdentifier } from '../types.js';
export declare class SynapsePayments {
    private readonly _provider;
    private readonly _signer;
    private readonly _network;
    private readonly _disableNonceManager;
    private _usdfcContract;
    private _paymentsContract;
    static readonly USDFC: "USDFC";
    constructor(provider: ethers.Provider, signer: ethers.Signer, network: 'mainnet' | 'calibration', disableNonceManager: boolean);
    private _getUsdfcContract;
    private _getPaymentsContract;
    balance(token?: TokenIdentifier): Promise<bigint>;
    walletBalance(token?: TokenIdentifier): Promise<bigint>;
    decimals(token?: TokenIdentifier): number;
    allowance(token: TokenIdentifier, spender: string): Promise<bigint>;
    approve(token: TokenIdentifier, spender: string, amount: TokenAmount): Promise<string>;
    approveService(service: string, rateAllowance: TokenAmount, lockupAllowance: TokenAmount, token?: TokenIdentifier): Promise<string>;
    revokeService(service: string, token?: TokenIdentifier): Promise<string>;
    serviceApproval(service: string, token?: TokenIdentifier): Promise<{
        isApproved: boolean;
        rateAllowance: bigint;
        rateUsed: bigint;
        lockupAllowance: bigint;
        lockupUsed: bigint;
    }>;
    deposit(amount: TokenAmount, token?: TokenIdentifier): Promise<string>;
    withdraw(amount: TokenAmount, token?: TokenIdentifier): Promise<string>;
}
//# sourceMappingURL=payments.d.ts.map