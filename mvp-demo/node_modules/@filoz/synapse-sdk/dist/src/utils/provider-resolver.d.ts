import type { SPRegistryService } from '../sp-registry/index.ts';
import type { ProviderInfo } from '../sp-registry/types.ts';
import type { WarmStorageService } from '../warm-storage/index.ts';
export declare class ProviderResolver {
    private readonly warmStorage;
    private readonly spRegistry;
    constructor(warmStorage: WarmStorageService, spRegistry: SPRegistryService);
    getApprovedProviders(limit?: number): Promise<ProviderInfo[]>;
    getApprovedProvider(providerId: number): Promise<ProviderInfo | null>;
    getApprovedProvidersByIds(providerIds: number[]): Promise<(ProviderInfo | null)[]>;
    getApprovedProviderByAddress(address: string): Promise<ProviderInfo | null>;
    isProviderApproved(providerId: number): Promise<boolean>;
}
//# sourceMappingURL=provider-resolver.d.ts.map