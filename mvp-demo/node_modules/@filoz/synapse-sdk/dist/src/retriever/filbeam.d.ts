import type { FilecoinNetworkType, PieceCID, PieceRetriever } from '../types.ts';
export declare class FilBeamRetriever implements PieceRetriever {
    private readonly baseRetriever;
    private readonly network;
    constructor(baseRetriever: PieceRetriever, network: FilecoinNetworkType);
    hostname(): string;
    fetchPiece(pieceCid: PieceCID, client: string, options?: {
        providerAddress?: string;
        withCDN?: boolean;
        signal?: AbortSignal;
    }): Promise<Response>;
}
//# sourceMappingURL=filbeam.d.ts.map