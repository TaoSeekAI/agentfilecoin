import type { PieceCID, PieceRetriever, ProviderInfo, SubgraphRetrievalService } from '../types.ts';
export declare class SubgraphRetriever implements PieceRetriever {
    private readonly subgraphService;
    private readonly childRetriever?;
    constructor(subgraphService: SubgraphRetrievalService, childRetriever?: PieceRetriever);
    findProviders(pieceCid: PieceCID, providerAddress?: string): Promise<ProviderInfo[]>;
    fetchPiece(pieceCid: PieceCID, client: string, options?: {
        providerAddress?: string;
        signal?: AbortSignal;
    }): Promise<Response>;
}
//# sourceMappingURL=subgraph.d.ts.map