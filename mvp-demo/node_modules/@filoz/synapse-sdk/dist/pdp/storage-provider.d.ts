import { type Signer, type ContractTransactionResponse } from 'ethers';
export interface ApprovedProviderInfo {
    owner: string;
    pdpUrl: string;
    pieceRetrievalUrl: string;
    registeredAt: bigint;
    approvedAt: bigint;
}
export interface PendingProviderInfo {
    pdpUrl: string;
    pieceRetrievalUrl: string;
    registeredAt: bigint;
}
export declare class StorageProviderTool {
    private readonly contract;
    private readonly signer;
    constructor(contractAddress: string, signer: Signer);
    register(pdpUrl: string, pieceRetrievalUrl: string): Promise<ContractTransactionResponse>;
    approve(providerAddress: string): Promise<ContractTransactionResponse>;
    reject(providerAddress: string): Promise<ContractTransactionResponse>;
    remove(providerId: bigint): Promise<ContractTransactionResponse>;
    isApproved(providerAddress: string): Promise<boolean>;
    getProviderIdByAddress(providerAddress: string): Promise<bigint>;
    getApprovedProvider(providerId: bigint): Promise<ApprovedProviderInfo>;
    getPendingProvider(providerAddress: string): Promise<PendingProviderInfo>;
    getNextProviderId(): Promise<bigint>;
    getOwner(): Promise<string>;
    getSignerAddress(): Promise<string>;
    isOwner(): Promise<boolean>;
    getAllApprovedProviders(): Promise<Array<{
        id: bigint;
        info: ApprovedProviderInfo;
    }>>;
    getContractAddress(): string;
}
//# sourceMappingURL=storage-provider.d.ts.map